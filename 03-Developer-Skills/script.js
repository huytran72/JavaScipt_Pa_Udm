// Remember, we're gonna use strict mode in all scripts now!
'use strict';

//Challenge #1
console.log('Challenge #1');

const arr = [17, 21, 23];

const printForecast = function(arr) {
    console.log(`${arr[1]} C in 1 days. `);
}